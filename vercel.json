{
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm run build --workspace=packages/db && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
